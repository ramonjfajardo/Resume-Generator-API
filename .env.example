# Environment Configuration Example
# Copy this file to .env.local and fill in your API keys
# .env.local is ignored by git and contains your secret keys

# OpenAI Configuration (Required if using OpenAI)
OPENAI_API_KEY=your_openai_api_key_here
# Optional: specify OpenAI model (defaults to gpt-4-turbo-preview)
OPENAI_MODEL=gpt-5.2-chat-latest

# Anthropic/Claude Configuration (Required if using Claude)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# Optional: specify Anthropic model (defaults to claude-haiku-4-5-20251001)
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# Notes:
# - The app defaults to OpenAI (provider="openai")
# - You only need the API key for the provider you're using
# - Keep your API keys secret - never commit .env.local to git
# - For Render.com: add these as environment variables in the service settings
